@model IEnumerable<entity_rafael.Models.Esporte>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Atleta.nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Atleta.nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEsporte }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEsporte }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEsporte })
        </td>
    </tr>
}

</table>
